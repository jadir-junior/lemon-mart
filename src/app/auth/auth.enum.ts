export enum Role {
  None = 'none',
  Clerk = 'clerk',
  Cashier = 'cashier',
  Manager = 'manager',
}

export enum AuthMode {
  InMemory = 'In Memory',
  CustomServicer = 'Custom Server',
  Firebase = 'Firebase',
}
